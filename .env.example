# Multi-API Configuration
# Define your APIs here - each API will be available at /api/{API_NAME}
# Format: API_CONFIGS='{API_NAME}:{API_URL}:{LABEL},{API_NAME2}:{API_URL2}:{LABEL2}'
# For APIs requiring auth tokens, add them at runtime in the UI (not in env vars for security)

API_CONFIGS=infractions:https://api.infractions.com:prod,oathkeeper:https://oathkeeper.api.com:prod

# Development example (localhost):
# API_CONFIGS=infractions:http://localhost:8000:local,oathkeeper:http://localhost:8001:local

# Example with multiple APIs:
# API_CONFIGS=infractions:https://api.infractions.com:prod,users:https://api.users.com:prod,orders:https://api.orders.com:prod

# Default API configuration
DEFAULT_TENANT=business
DEFAULT_LANGUAGE=en

# Note: Bearer tokens should be entered at runtime in the API Tester for security
# Do not store authentication tokens in environment variables
